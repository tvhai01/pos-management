"""
Shared package — cross-cutting concerns for the POS Management System.

Contains base models, response helpers, exception handlers, pagination,
and global constants used across all Django apps.
"""
